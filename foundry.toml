[profile.default]
src = "src"
out = "out"
libs = ["lib"]

remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/",
    "forge-std/=lib/forge-std/src/"
]

# --- ADD THESE LINES ---
via_ir = true
optimizer = true
optimizer_runs = 200
# -----------------------